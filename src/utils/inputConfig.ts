export type InputConfig = {
  size: 'small' | 'medium';
  variant: 'outlined';
  fullWidth: boolean;
};

const inputConfig: InputConfig = { size: 'small', variant: 'outlined', fullWidth: true };
export default inputConfig;
